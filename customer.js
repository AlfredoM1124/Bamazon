// Declaring npm dependencies
var inquirer = require("inquirer");
var mysql = require("mysql");
// Constructor that defines customer's accessibility options 
var customer = function (username) {

	this.username = username;
	this.totalPrice = "";
	var itemCart = [];
	var options = [];

	var 
}